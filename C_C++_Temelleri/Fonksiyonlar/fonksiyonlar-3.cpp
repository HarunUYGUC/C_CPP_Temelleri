// (5! + 7!) / (9! - 6!) iþlemini yapan programý yazýnýz.

/*
#include <iostream>
using namespace std;

int faktoriyel(int sayi)
{
	int i, fak = 1;

	for (i = 1; i <= sayi; i++)
	{
		fak *= i;
	}

	return fak;
}

int main()
{
	int a = 5, b = 7, c = 9, d = 6;
	double sonuc;

	sonuc = double(faktoriyel(a) + faktoriyel(b)) / (faktoriyel(c) - faktoriyel(d));
	
	cout << "(5! + 7!) / (9! - 6!) iþlemininin sonucu = " << sonuc;
}
*/
