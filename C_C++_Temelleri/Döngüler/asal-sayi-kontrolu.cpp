/*
#include <iostream>
using namespace std;

int main()
{
	setlocale(LC_ALL, "Turkish");
	int sayi, bolen, bayrak = true;

	cout << "Pozitif bir tam sayý giriniz: ";
	cin >> sayi;

	for (bolen = 2; bolen <= (sayi / 2); bolen++)
	{
		if (sayi % bolen == 0)
		{	
			cout << sayi << " asal sayý deðildir.";
			bayrak = false;
			break;
		}
	}

	if (bayrak == true)
	{
		cout << sayi << " asal sayýdýr";
	}
}
*/
