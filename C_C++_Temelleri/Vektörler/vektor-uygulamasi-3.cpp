/*
#include <iostream>
#include <vector>
using namespace std;

void guncelle(vector <int> &rakamlar)
{
	for (int i = 0; i < rakamlar.size(); i++)
		rakamlar[i] *= 10;
}

int main()
{
	setlocale(LC_ALL, "Turkish");
	vector <int> sayilar = { 1, 2, 3, 4, 5, 6, 7, 8 };
	
	cout << "Vektörün Ýlk Hali: " << endl;
	for (int sayi : sayilar)
	{
		cout << sayi << endl;
	}

	guncelle(sayilar);

	cout << "Vektörün Son Hali: " << endl;
	for (int sayi : sayilar)
	{
		cout << sayi << endl;
	}

	return 0;
}
*/
