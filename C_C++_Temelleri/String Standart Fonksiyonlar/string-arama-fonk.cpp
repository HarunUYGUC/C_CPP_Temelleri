/*

strchr(char s1[], char c1) => c1 karakterinin s1 stringi içinde yer alýp almadýðýný kontrol eder. 
Eðer yer alýyorsa pointer deðiþkenine yer aldýðý ilk noktanýn adresini döndürür.

strrchr(char s1[], char c1) => c1 karakterini s1 string ifadesinin sonundan arama yapar.
Eðer yer alýyorsa pointer deðiþkenine yer aldýðý ilk noktanýn adresini döndürür.

strstr(char s1[], char s2[]) => s2 stringinde yer alan karakterlerden s1 stringinde yer alýp almadýðýný kontrol eder.
Karakterlerin yer almasý durumunda pointer deðiþkenine ilk konum bilgisini atar.

strcspn(char s1[], char s2[]) => bir karakter dizisi içindeki karakterlerin herhangi birinin diðer karakter dizisi
içinde bulunduðu ilk yerin indeksini hesaplar.

*/

/*
#include <iostream>
#include <cstdlib>
#include <cstring>
using namespace std;

int main()
{
	setlocale(LC_ALL, "Turkish");
	//char str[] = "Bilisim Dunyasi Udemy Kursu";
	//char* ptr;

	//ptr = strchr(str, 'm');
	//printf("%x \n", ptr);
	//printf("%s", ptr);

	//ptr = strrchr(str, 'm');
	//printf("%x \n", ptr);
	//printf("%s", ptr);

	//ptr = strstr(str, "Dunyasi");
	//printf("%x \n", ptr);
	//printf("%s", ptr);

	//ptr = strstr(str, "program");
	//printf("%x \n", ptr);
	//printf("%s", ptr);


	//char str[] = "Bilisim Dunyasi Udemy Kursu";
	//int konum;

	//konum = strcspn(str, "Uum");
	//printf("%d", konum);

	return 0;
}
*/
