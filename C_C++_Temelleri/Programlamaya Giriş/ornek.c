/*
#include <stdio.h>
#include <locale.h>

int main()
{
	setlocale(LC_ALL, "Turkish");
	float kisaKenar, uzunKenar, cevre, alan;

	printf("Dikdörtgenin kýsa kenarýný giriniz: ");
	scanf_s("%f", &kisaKenar);

	printf("Dikdörtgenin uzun kenarýný giriniz: ");
	scanf_s("%f", &uzunKenar);

	cevre = 2 * (kisaKenar + uzunKenar);
	alan = kisaKenar * uzunKenar;

	printf("Dikdörtgenin çevresi = %f \n", cevre);
	printf("Dikdörtgenin alaný = %f", alan);
}
*/

/*
#include <stdio.h>
#include <locale.h>
#include <math.h>
#define PII 3.14

int main()
{
	setlocale(LC_ALL, "Turkish");
	float r, alan;
	
	printf("Çemberin yarýçapýný giriniz: ");
	scanf_s("%f", &r);

	alan = PII * pow(r, 2);
	printf("Çemberin alaný = %f", alan);
}
*/
